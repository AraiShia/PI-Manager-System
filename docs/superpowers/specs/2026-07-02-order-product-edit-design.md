# 订单产品编辑功能设计规格

**日期**: 2026-07-02  
**主题**: 编辑订单产品 Dialog、表格内联编辑、右键菜单与采购/库存联动  
**相关文件**:
- `client/widgets/order_summary/order_detail_panel.py`
- `client/widgets/product_item_edit_dialog.py`
- `client/main.py`
- `backend/crud/pi.py`
- `backend/routers/pi.py`

---

## 1. 目标

让订单详情中的产品项支持灵活编辑：
- 简单数字/文本字段在表格内联编辑，失焦自动保存。
- 列 2-9、交货日期、包装规格等通过"编辑产品"Dialog 编辑。
- 采购/工厂/供应商相关字段通过"更换供应商"Dialog 编辑。
- 右键菜单识别当前列，打开 Dialog 后高亮对应字段。
- 采购后锁定产品基础信息，但允许通过"更换供应商"Dialog 更换工厂/店铺并重新生成采购单。

---

## 2. 字段编辑方式

### 2.1 表格内联编辑（失焦自动保存）

| 表格列 | 表头 | 字段名 | 控件 |
|---|---|---|---|
| 10 | 报价(USD/RMB) | `unit_price` | 数字输入 |
| 13 | 客户预付款 | `customer_prepayment` | 数字输入 |
| 14 | 待收尾款 | `remaining_payment` | 数字输入 |
| 18 | 运费 | `shipping_fee` | 数字输入 |
| 19 | 杂费 | `misc_fee` | 数字输入 |

- 仅这 5 列设置 `Qt.ItemIsEditable` 标志。
- 双击单元格进入编辑，失去焦点时调用 `PUT /pi/items/{item_id}`。
- 只提交变更的字段，失败时弹窗提示并回滚显示。

### 2.2 编辑产品 Dialog（右键"编辑产品"）

Dialog 初始尺寸: **768 × 1280**。

| 分组 | 表格列 | 表头 | 字段名 | 控件 |
|---|---|---|---|---|
| 基础信息 | 2 | 客户产品编号 | `customer_code` / `customer_model` | 文本 |
| | 3 | OE号 | `oe_number` | 文本 |
| | 4 | 客户需求/产品备注 | `customer_remark` / `remark` | 文本 |
| | 5 | 产品名称 | `product_name` / `detail_desc` | 文本 |
| | 6 | 图片 | `image_url` / `default_image_url` | 图片上传/清除 |
| | 7 | 客户型号 | `customer_model` | 文本 |
| | 8 | 产品特性 | `product_feature` | 文本 |
| 数量与日期 | 9 | 数量 | `quantity` | 数字 |
| | 23 | 交货日期 | `delivery_date` | 日期选择器 |
| 包装规格 | 29 | 包装方式 | `packaging` / `packing_type` | 下拉 |
| | 30 | 采购选项/名称 | `purchase_option_name` | 文本 |
| | 31 | 产品细节 | `product_detail` / `product_spec` | 文本 |
| | 33 | 纸箱尺寸 | `carton_size` | 文本（LxWxH） |
| | 34 | 打包规格 | `pack_spec` / `packing_spec` | 文本 |
| | 37 | 整箱毛重 | `carton_gross_weight` | 数字 |
| 客户回复 | 12 | 最新客户回复 | `latest_customer_reply` | 只读列表 + 新增输入框 |

- 右键某列打开 Dialog 时，若该列属于 Dialog 内字段，滚动到对应控件并高亮。
- 客户回复区域为预留接口：当前仅本地显示，新增回复调用 `POST /pi/items/{item_id}/replies`（待实现），失败时本地记录并提示。

### 2.3 更换供应商 Dialog（右键"更换供应商"）

| 分组 | 表格列 | 表头 | 字段名 | 控件 |
|---|---|---|---|---|
| 供应商信息 | 21 | 工厂简称 | `factory_short_name` / `supplier_name` | 文本 |
| | 22 | 店铺链接 | `supplier_url` / `shop_url` | 文本 |
| | 32 | 工厂编号 | `factory_code` | 文本 |
| | 39 | 品牌 | `brand` | 文本 |
| 采购价格 | 17 | 采购价格 | `purchase_price` / `factory_price` | 数字 |
| | 25 | 工厂订金 | `factory_deposit` | 数字 |
| | 26 | 工厂尾款 | `factory_balance` | 数字 |
| 开票 | 40 | 开票情况 | `invoice_status` / `invoice_type` | 下拉 |

- 右键采购/工厂/供应商相关列时，打开此 Dialog 并高亮对应字段。
- 保存后重新生成采购单（见 3.2 节）。

### 2.4 不编辑字段

| 类型 | 列 | 说明 |
|---|---|---|
| 订单级 | 0 订单日期、1 ORDER NO. | 随 PI 主单编辑 |
| 派生 | 11 合计金额、15 预估美金报价、16 预估毛利率、20 总金额、35 箱数、36 预估体积、38 总重量 | 由其他字段计算 |
| 流程状态 | 24 是否已收货、27 入库操作、28 入库数量 | 由采购/入库流程写入 |

---

## 3. 右键菜单

在现有右键菜单基础上增加两项：

```
采购该产品
重新采购
─────────
入库该产品
删除商品
─────────
编辑产品                ← 新增
更换供应商              ← 新增
─────────
缺货标记
```

- 点击"编辑产品"/"更换供应商"时记录当前右键列号。
- 根据列号决定打开哪个 Dialog 以及高亮哪个字段。

---

## 4. 锁定规则

### 4.1 未采购时

- 编辑产品 Dialog：全部字段可编辑。
- 更换供应商 Dialog：全部字段可编辑。

### 4.2 已采购后

- 编辑产品 Dialog：
  - 锁定：列 2-9、23 交货日期、包装规格字段（29/30/31/33/34/37）。
  - 可编辑：客户回复（新增回复）。
- 更换供应商 Dialog：
  - 全部字段解锁，允许更换工厂/店铺/价格/开票信息。
  - 保存后重新生成采购单。

---

## 5. 保存与联动

### 5.1 后端 API

继续复用 `PUT /pi/items/{item_id}`，由 `update_pi_item` 处理字段更新。

### 5.2 编辑产品 Dialog 保存

- 一次性提交所有变更字段。
- `quantity` / `unit_price` 变化时，后端自动：
  - 重算 `total_price`。
  - 刷新 PI 主单 `total_amount`。
- `quantity` 减少时，后端检查 `InvInventory`：
  - 若库存数量 > 新数量，返回 400 并提示"请先处理库存"，前端保持原值。
  - 若库存数量 ≤ 新数量，更新库存数量为新的订单数量（或标记待核对，视业务决定）。

### 5.3 更换供应商 Dialog 保存

- 提交供应商相关字段。
- 后端逻辑：
  1. 更新 PI item 的供应商字段。
  2. 查找该产品项关联的采购单（`PoPurchaseOrderItem`）。
  3. 若采购单状态为"未下单"/"已下单未收货"：
     - 删除原采购单及采购项，根据新供应商信息重新创建采购单。
  4. 若采购单已收货/已入库：
     - 返回 400，提示"已收货/入库，无法更换供应商，请先处理库存"。
  5. 更新库存关联记录的供应商信息（如果有）。

### 5.4 表格内联保存

- 失焦时立即调用 `PUT /pi/items/{item_id}`，只传修改字段。
- 保存成功后刷新当前行显示；失败时回滚并提示。

---

## 6. UI/UX 细节

### 6.1 Dialog 高亮

- 打开 Dialog 时，根据传入的 `focus_field` 或 `focus_column`：
  - 滚动到对应分组。
  - 对输入框设置黄色边框背景 2 秒。
  - 如果是下拉框，展开下拉列表。

### 6.2 图片上传

- 编辑产品 Dialog 中图片区域显示当前图片缩略图。
- 点击"上传图片"调用现有图片上传接口，成功后更新 `image_url`。
- 点击"清除图片"移除图片（后端字段置空）。

### 6.3 日期选择器

- 交货日期使用 `QDateEdit`，格式 `yyyy-MM-dd`。

### 6.4 下拉选项

- 包装方式：`["1件/箱", "多件/箱", "1件多箱"]`
- 开票情况：`["增票", "普票", "不开票"]`

---

## 7. 实现范围

本次实现包括：
1. `order_detail_panel.py`：表格可编辑标志、失焦保存、右键菜单扩展。
2. `product_item_edit_dialog.py`：扩展为完整编辑产品 Dialog（768×1280）。
3. 新建 `supplier_change_dialog.py`：更换供应商 Dialog。
4. `backend/crud/pi.py` / `backend/routers/pi.py`：必要时扩展字段更新和采购单重新生成逻辑。
5. 预留客户回复接口。

本次不实现：
- 客户回复后端接口（仅预留）。
- 订单级字段编辑。
- 自动派生字段手动编辑。
