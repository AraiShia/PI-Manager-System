# 订单管理总表 - 待完成功能实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 完成订单管理总表的 FileUploadDialog 组件、计算公式、备忘录关联功能

**架构：** 统一弹窗 + 类型筛选模式，MemoDialog 通过 entity_type/entity_id/field_name 关联订单项，FileUploadDialog 通过 file_type 参数筛选文件类型

**技术栈：** Python/FastAPI（后端），PySide6（前端）

---

## 文件结构

### 前端文件
- `client/widgets/file_upload_dialog.py` - 文件上传弹窗组件（新建）
- `client/main.py` - 修改订单详情表格双击事件处理（修改）

### 后端文件
- `backend/routers/payment.py` - 添加按 PI 查询付款的 API（修改）
- `client/api/client.py` - 添加付款查询 API（修改）

---

## 任务 1：FileUploadDialog 组件

**文件：**
- 创建：`client/widgets/file_upload_dialog.py`

- [ ] **步骤 1：创建 FileUploadDialog 组件基础结构**

```python
# client/widgets/file_upload_dialog.py
from PySide6.QtWidgets import (
    QDialog, QVBoxLayout, QHBoxLayout, QPushButton,
    QTableWidget, QTableWidgetItem, QHeaderView, QLabel,
    QMessageBox, QFileDialog, QGroupBox
)
from PySide6.QtCore import Qt
from PySide6.QtGui import QFont, QIcon
import os


class FileUploadDialog(QDialog):
    FILE_TYPE_LABELS = {
        "contract": "合同文件",
        "invoice": "发票文件",
        "customs": "报关单"
    }

    def __init__(self, api_client, pi_id, pi_no, file_type=None, parent=None):
        super().__init__(parent)
        self.api_client = api_client
        self.pi_id = pi_id
        self.pi_no = pi_no
        self.file_type = file_type
        self.files = []
        self.init_ui()
        self.load_files()

    def init_ui(self):
        title = self.FILE_TYPE_LABELS.get(self.file_type, "文件管理")
        self.setWindowTitle(f"{title} - {self.pi_no}")
        self.setMinimumSize(600, 400)

        layout = QVBoxLayout()

        header = QLabel(f"当前订单: {self.pi_no}")
        header.setFont(QFont("", 10))
        layout.addWidget(header)

        self.tabs = {}
        if not self.file_type:
            for ftype, label in self.FILE_TYPE_LABELS.items():
                group = QGroupBox(label)
                group_layout = QVBoxLayout()
                table = QTableWidget()
                table.setColumnCount(4)
                table.setHorizontalHeaderLabels(["文件名", "上传时间", "大小", "操作"])
                table.horizontalHeader().setSectionResizeMode(QHeaderView.ResizeMode.Stretch)
                table.verticalHeader().setVisible(False)
                group_layout.addWidget(table)
                group.setLayout(group_layout)
                layout.addWidget(group)
                self.tabs[ftype] = table
        else:
            self.current_group = QGroupBox(title)
            group_layout = QVBoxLayout()
            self.current_table = QTableWidget()
            self.current_table.setColumnCount(4)
            self.current_table.setHorizontalHeaderLabels(["文件名", "上传时间", "大小", "操作"])
            self.current_table.horizontalHeader().setSectionResizeMode(QHeaderView.ResizeMode.Stretch)
            self.current_table.verticalHeader().setVisible(False)
            group_layout.addWidget(self.current_table)
            self.current_group.setLayout(group_layout)
            layout.addWidget(self.current_group)

        btn_layout = QHBoxLayout()
        if self.file_type:
            upload_btn = QPushButton("上传文件")
            upload_btn.clicked.connect(self.upload_file)
            btn_layout.addWidget(upload_btn)

        close_btn = QPushButton("关闭")
        close_btn.clicked.connect(self.accept)
        btn_layout.addWidget(close_btn)
        layout.addLayout(btn_layout)

        self.setLayout(layout)

    def load_files(self):
        try:
            if self.file_type:
                self.files = self.api_client.get_order_files(self.pi_id, self.file_type)
                self.populate_table(self.current_table, self.files)
            else:
                for ftype, table in self.tabs.items():
                    files = self.api_client.get_order_files(self.pi_id, ftype)
                    self.populate_table(table, files)
        except Exception as e:
            QMessageBox.warning(self, "错误", f"加载文件失败: {e}")

    def populate_table(self, table, files):
        table.setRowCount(len(files))
        for i, f in enumerate(files):
            table.setItem(i, 0, QTableWidgetItem(f.get('original_name', '')))
            table.setItem(i, 1, QTableWidgetItem(str(f.get('uploaded_at', ''))[:19]))
            size = f.get('file_size', 0)
            table.setItem(i, 2, QTableWidgetItem(self._format_size(size)))

            btn_layout = QHBoxLayout()
            btn_layout.setContentsMargins(0, 0, 0, 0)

            download_btn = QPushButton("下载")
            download_btn.clicked.connect(lambda checked, fid=f['id']: self.download_file(fid))
            delete_btn = QPushButton("删除")
            delete_btn.clicked.connect(lambda checked, fid=f['id']: self.delete_file(fid))

            widget = QWidget()
            layout = QHBoxLayout(widget)
            layout.addWidget(download_btn)
            layout.addWidget(delete_btn)
            layout.setContentsMargins(0, 0, 0, 0)
            table.setCellWidget(i, 3, widget)

    def _format_size(self, size):
        if size < 1024:
            return f"{size}B"
        elif size < 1024 * 1024:
            return f"{size / 1024:.1f}KB"
        else:
            return f"{size / (1024 * 1024):.1f}MB"

    def upload_file(self):
        file_path, _ = QFileDialog.getOpenFileName(
            self,
            "选择文件",
            "",
            "支持的文件 (*.pdf *.doc *.docx *.jpg *.jpeg *.png *.xls *.xlsx)"
        )
        if not file_path:
            return

        try:
            self.api_client.upload_order_file(self.pi_id, self.file_type, file_path)
            QMessageBox.information(self, "成功", "文件上传成功")
            self.load_files()
        except Exception as e:
            QMessageBox.warning(self, "错误", f"上传失败: {e}")

    def download_file(self, file_id):
        try:
            from PySide6.QtWidgets import QFileDialog
            save_path, _ = QFileDialog.getSaveFileName(self, "保存文件", "", "所有文件 (*.*)")
            if save_path:
                self.api_client.download_order_file(file_id, save_path)
                QMessageBox.information(self, "成功", f"文件已保存到: {save_path}")
        except Exception as e:
            QMessageBox.warning(self, "错误", f"下载失败: {e}")

    def delete_file(self, file_id):
        reply = QMessageBox.question(
            self, "确认删除", "确定删除这个文件吗?",
            QMessageBox.StandardButton.Yes | QMessageBox.StandardButton.No
        )
        if reply == QMessageBox.StandardButton.Yes:
            try:
                self.api_client.delete_order_file(file_id)
                self.load_files()
            except Exception as e:
                QMessageBox.warning(self, "错误", f"删除失败: {e}")
```

- [ ] **步骤 2：在 client/main.py 中添加文件下载方法**

在 ApiClient 类中添加：
```python
def download_order_file(self, file_id: int, save_path: str):
    """下载订单文件"""
    url = self._build_url(f"/order-files/download/{file_id}")
    response = self.session.get(url, timeout=REQUEST_TIMEOUT)
    response.raise_for_status()
    with open(save_path, 'wb') as f:
        f.write(response.content)
```

- [ ] **步骤 3：测试组件**

手动测试：
1. 打开订单详情
2. 点击文件管理列
3. 验证弹窗显示正确

- [ ] **步骤 4：Commit**

```bash
git add client/widgets/file_upload_dialog.py client/api/client.py
git commit -m "feat: 添加文件上传弹窗组件"
```

---

## 任务 2：订单详情表格双击事件处理

**文件：**
- 修改：`client/main.py`

- [ ] **步骤 1：查找并修改订单详情表格双击事件**

找到 `_open_field_edit_dialog` 方法，添加备忘录弹窗逻辑：

```python
def _open_field_edit_dialog(self, row, col):
    """打开字段编辑对话框"""
    order = self._current_order
    if not order:
        return

    items = order.get('items', [])
    if row >= len(items):
        return

    item = items[row]
    pi_item_id = item.get('id')

    # 列索引对应关系（根据 order_detail_table 的列定义）
    # 需要根据实际列顺序调整
    memo_fields = {
        4: 'remark',           # 客户需求备注
        8: 'specifications',   # 产品特性
        12: 'customer_reply',  # 客户回复
        31: 'detail_desc',     # 产品细节
    }

    file_fields = {
        # 'contract': 'contract',   # 合同
        # 'invoice': 'invoice',     # 发票
        # 'customs': 'customs',     # 报关单
    }

    if col in memo_fields:
        field_name = memo_fields[col]
        from widgets.memo_dialog import MemoDialog
        dialog = MemoDialog(
            self.api_client,
            entity_type="pi_item",
            entity_id=pi_item_id,
            field_name=field_name,
            parent=self
        )
        dialog.exec()
    elif col in file_fields:
        file_type = file_fields[col]
        from widgets.file_upload_dialog import FileUploadDialog
        dialog = FileUploadDialog(
            self.api_client,
            pi_id=order.get('id'),
            pi_no=order.get('pi_no', ''),
            file_type=file_type,
            parent=self
        )
        dialog.exec()
    else:
        # 原有的编辑逻辑
        self._edit_cell(row, col)
```

- [ ] **步骤 2：Commit**

```bash
git add client/main.py
git commit -m "feat: 订单详情表格双击打开备忘录和文件弹窗"
```

---

## 任务 3：计算公式实现

**文件：**
- 修改：`client/main.py`

- [ ] **步骤 1：添加计算字段显示**

在加载订单详情数据后，计算并添加显示字段：

```python
def _calculate_order_detail_fields(self, order, item):
    """计算订单详情中的派生字段"""
    quantity = float(item.get('quantity', 0) or 0)
    unit_price = float(item.get('unit_price', 0) or 0)
    total_price = quantity * unit_price

    purchase_price = float(item.get('purchase_price', 0) or 0)
    purchase_quantity = float(item.get('purchase_quantity', 0) or quantity)
    freight = float(item.get('freight', 0) or 0)
    packing_fee = float(item.get('packing_fee', 0) or 0)

    purchase_total = purchase_price * purchase_quantity + freight + packing_fee

    exchange_rate = getattr(self, '_current_exchange_rate', 6.8)
    profit_margin = getattr(self, '_current_profit_margin', 0.2)

    estimated_usd = (purchase_price * (1 + profit_margin)) / exchange_rate if exchange_rate else 0
    estimated_margin = (unit_price * exchange_rate / total_price - 1) * 100 if total_price and exchange_rate else 0

    box_count = float(item.get('box_count', 0) or 0)
    weight = float(item.get('weight', 0) or 0)
    total_weight = box_count * weight

    return {
        'total_price_display': total_price,
        'purchase_total_display': purchase_total,
        'estimated_usd_display': estimated_usd,
        'estimated_margin_display': estimated_margin,
        'total_weight_display': total_weight,
    }
```

- [ ] **步骤 2：在加载订单详情时调用计算方法**

找到 `_load_order_detail` 或类似方法，在组装数据时调用：

```python
# 在组装 item 数据后添加计算字段
calculated = self._calculate_order_detail_fields(order, item)
item.update(calculated)
```

- [ ] **步骤 3：Commit**

```bash
git add client/main.py
git commit -m "feat: 添加订单详情计算公式"
```

---

## 任务 4：添加按 PI 查询付款的 API

**文件：**
- 修改：`backend/routers/payment.py`
- 修改：`client/api/client.py`

- [ ] **步骤 1：添加后端 API**

```python
# backend/routers/payment.py 添加
@router.get("/customer/by-pi/{pi_id}")
def get_customer_payments_by_pi(pi_id: int, db: Session = Depends(get_db)):
    """按 PI 获取客户付款记录"""
    return payment_crud.get_customer_payments_by_pi(db, pi_id)

@router.get("/supplier/by-pi/{pi_id}")
def get_supplier_payments_by_pi(pi_id: int, db: Session = Depends(get_db)):
    """按 PI 获取供应商付款记录"""
    return payment_crud.get_supplier_payments_by_pi(db, pi_id)
```

- [ ] **步骤 2：添加 CRUD 方法**

```python
# backend/crud/payment.py 添加
def get_customer_payments_by_pi(db: Session, pi_id: int):
    return db.query(ArCustomerPayment).filter(
        ArCustomerPayment.pi_id == pi_id
    ).order_by(ArCustomerPayment.payment_date.desc()).all()

def get_supplier_payments_by_pi(db: Session, pi_id: int):
    return db.query(ApSupplierPayment).filter(
        ApSupplierPayment.pi_id == pi_id
    ).order_by(ApSupplierPayment.payment_date.desc()).all()
```

- [ ] **步骤 3：添加前端 API**

```python
# client/api/client.py 添加
def get_customer_payments_by_pi(self, pi_id: int) -> List[Dict]:
    return self.get(f"/payments/customer/by-pi/{pi_id}") or []

def get_supplier_payments_by_pi(self, pi_id: int) -> List[Dict]:
    return self.get(f"/payments/supplier/by-pi/{pi_id}") or []
```

- [ ] **步骤 4：Commit**

```bash
git add backend/routers/payment.py backend/crud/payment.py client/api/client.py
git commit -m "feat: 添加按PI查询付款记录API"
```

---

## 实施优先级

| 任务 | 优先级 | 说明 |
|:---:|:---:|:---|
| 1 | P1 | FileUploadDialog 组件 |
| 2 | P2 | 订单详情表格双击事件 |
| 3 | P3 | 计算公式实现 |
| 4 | P4 | 付款查询 API |

---

**计划已完成并保存到 `docs/superpowers/plans/2026-05-27-订单管理总表待完成功能.md`。**

**两种执行方式：**

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

**选哪种方式？**