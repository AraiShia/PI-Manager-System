# 订单管理总表实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 实现订单管理总表，替代 Excel 进行订单管理

**架构：** 基于现有 PI Manager 系统，在订单总表模块中整合客户产品、采购、付款等数据，提供备忘录、往来款、文件上传等可复用组件。

**技术栈：** Python/FastAPI（后端），PySide6（前端），SQLite（数据库）

---

## 文件结构

### 后端文件
- `backend/models/memo_record.py` - 备忘录数据模型
- `backend/models/order_file.py` - 订单文件数据模型
- `backend/crud/memo_record.py` - 备忘录 CRUD
- `backend/crud/order_file.py` - 订单文件 CRUD
- `backend/routers/memo_record.py` - 备忘录 API 路由
- `backend/routers/order_file.py` - 订单文件 API 路由
- `backend/schemas/memo_record.py` - 备忘录 Schema
- `backend/schemas/order_file.py` - 订单文件 Schema

### 前端文件
- `client/widgets/memo_dialog.py` - 备忘录弹窗组件
- `client/widgets/payment_dialog.py` - 往来款弹窗组件
- `client/widgets/file_upload_dialog.py` - 文件上传弹窗组件
- `client/main.py` - 修改订单总表实现

---

## 任务 1：备忘录数据模型和 API

**文件：**
- 创建：`backend/models/memo_record.py`
- 创建：`backend/schemas/memo_record.py`
- 创建：`backend/crud/memo_record.py`
- 创建：`backend/routers/memo_record.py`
- 修改：`backend/models/__init__.py`
- 修改：`backend/schemas/__init__.py`

- [ ] **步骤 1：创建备忘录数据模型**

```python
# backend/models/memo_record.py
from sqlalchemy import Column, Integer, String, Text, DateTime, ForeignKey
from sqlalchemy.orm import relationship
from app.database import Base
from datetime import datetime

class MemoRecord(Base):
    __tablename__ = "memo_record"

    id = Column(Integer, primary_key=True, autoincrement=True)
    entity_type = Column(String(50), nullable=False, index=True)
    entity_id = Column(Integer, nullable=False, index=True)
    field_name = Column(String(50), nullable=False)
    content = Column(Text)
    created_by = Column(Integer)
    created_at = Column(DateTime, default=datetime.now)
    updated_at = Column(DateTime, default=datetime.now, onupdate=datetime.now)
```

- [ ] **步骤 2：创建备忘录 Schema**

```python
# backend/schemas/memo_record.py
from pydantic import BaseModel
from datetime import datetime
from typing import Optional

class MemoRecordCreate(BaseModel):
    entity_type: str
    entity_id: int
    field_name: str
    content: str

class MemoRecordUpdate(BaseModel):
    content: Optional[str] = None

class MemoRecordResponse(BaseModel):
    id: int
    entity_type: str
    entity_id: int
    field_name: str
    content: Optional[str]
    created_by: Optional[int]
    created_at: datetime
    updated_at: Optional[datetime]

    class Config:
        from_attributes = True
```

- [ ] **步骤 3：创建备忘录 CRUD**

```python
# backend/crud/memo_record.py
from sqlalchemy.orm import Session
from models.memo_record import MemoRecord
from schemas.memo_record import MemoRecordCreate, MemoRecordUpdate

def create_memo(db: Session, memo: MemoRecordCreate, user_id: int = None) -> MemoRecord:
    db_memo = MemoRecord(
        entity_type=memo.entity_type,
        entity_id=memo.entity_id,
        field_name=memo.field_name,
        content=memo.content,
        created_by=user_id
    )
    db.add(db_memo)
    db.commit()
    db.refresh(db_memo)
    return db_memo

def get_memos(db: Session, entity_type: str, entity_id: int, field_name: str = None):
    query = db.query(MemoRecord).filter(
        MemoRecord.entity_type == entity_type,
        MemoRecord.entity_id == entity_id
    )
    if field_name:
        query = query.filter(MemoRecord.field_name == field_name)
    return query.order_by(MemoRecord.created_at.desc()).all()

def update_memo(db: Session, memo_id: int, memo: MemoRecordUpdate) -> MemoRecord:
    db_memo = db.query(MemoRecord).filter(MemoRecord.id == memo_id).first()
    if db_memo and memo.content is not None:
        db_memo.content = memo.content
        db.commit()
        db.refresh(db_memo)
    return db_memo

def delete_memo(db: Session, memo_id: int) -> bool:
    db_memo = db.query(MemoRecord).filter(MemoRecord.id == memo_id).first()
    if db_memo:
        db.delete(db_memo)
        db.commit()
        return True
    return False
```

- [ ] **步骤 4：创建备忘录路由**

```python
# backend/routers/memo_record.py
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from typing import List, Optional
from app.database import get_db
from crud import memo_record
from schemas.memo_record import MemoRecordCreate, MemoRecordUpdate, MemoRecordResponse

router = APIRouter(prefix="/api/memos", tags=["备忘录"])

@router.post("/", response_model=MemoRecordResponse)
def create_memo_api(memo: MemoRecordCreate, db: Session = Depends(get_db)):
    return memo_record.create_memo(db, memo)

@router.get("/", response_model=List[MemoRecordResponse])
def get_memos_api(
    entity_type: str,
    entity_id: int,
    field_name: Optional[str] = None,
    db: Session = Depends(get_db)
):
    return memo_record.get_memos(db, entity_type, entity_id, field_name)

@router.put("/{memo_id}", response_model=MemoRecordResponse)
def update_memo_api(memo_id: int, memo: MemoRecordUpdate, db: Session = Depends(get_db)):
    return memo_record.update_memo(db, memo_id, memo)

@router.delete("/{memo_id}")
def delete_memo_api(memo_id: int, db: Session = Depends(get_db)):
    memo_record.delete_memo(db, memo_id)
    return {"message": "删除成功"}
```

- [ ] **步骤 5：更新 __init__.py 导出**

```python
# backend/models/__init__.py
from .memo_record import MemoRecord
from .order_file import OrderFile
```

- [ ] **步骤 6：提交代码**

```bash
git add backend/models/memo_record.py backend/schemas/memo_record.py backend/crud/memo_record.py backend/routers/memo_record.py
git commit -m "feat: 添加备忘录数据模型和API"
```

---

## 任务 2：文件上传数据模型和 API

**文件：**
- 创建：`backend/models/order_file.py`
- 创建：`backend/schemas/order_file.py`
- 创建：`backend/crud/order_file.py`
- 创建：`backend/routers/order_file.py`

- [ ] **步骤 1：创建文件数据模型**

```python
# backend/models/order_file.py
from sqlalchemy import Column, Integer, String, DateTime, ForeignKey
from sqlalchemy.orm import relationship
from app.database import Base
from datetime import datetime

class OrderFile(Base):
    __tablename__ = "order_file"

    id = Column(Integer, primary_key=True, autoincrement=True)
    pi_id = Column(Integer, ForeignKey("pi_proforma_invoice.id"), nullable=False, index=True)
    product_id = Column(Integer, nullable=True, index=True)
    file_type = Column(String(50), nullable=False)  # contract, invoice, customs
    file_name = Column(String(255), nullable=False)
    file_path = Column(String(500), nullable=False)
    file_size = Column(Integer)
    file_ext = Column(String(20))
    uploaded_by = Column(Integer)
    uploaded_at = Column(DateTime, default=datetime.now)
```

- [ ] **步骤 2：创建文件 Schema 和 CRUD**

```python
# backend/schemas/order_file.py
from pydantic import BaseModel
from datetime import datetime
from typing import Optional

class OrderFileCreate(BaseModel):
    pi_id: int
    product_id: Optional[int] = None
    file_type: str
    file_name: str
    file_path: str
    file_size: Optional[int] = None
    file_ext: Optional[str] = None

class OrderFileResponse(BaseModel):
    id: int
    pi_id: int
    product_id: Optional[int]
    file_type: str
    file_name: str
    file_path: str
    file_size: Optional[int]
    file_ext: Optional[str]
    uploaded_by: Optional[int]
    uploaded_at: datetime

    class Config:
        from_attributes = True
```

```python
# backend/crud/order_file.py
from sqlalchemy.orm import Session
from models.order_file import OrderFile
from schemas.order_file import OrderFileCreate

def create_order_file(db: Session, file: OrderFileCreate, user_id: int = None) -> OrderFile:
    db_file = OrderFile(
        pi_id=file.pi_id,
        product_id=file.product_id,
        file_type=file.file_type,
        file_name=file.file_name,
        file_path=file.file_path,
        file_size=file.file_size,
        file_ext=file.file_ext,
        uploaded_by=user_id
    )
    db.add(db_file)
    db.commit()
    db.refresh(db_file)
    return db_file

def get_order_files(db: Session, pi_id: int, file_type: str = None):
    query = db.query(OrderFile).filter(OrderFile.pi_id == pi_id)
    if file_type:
        query = query.filter(OrderFile.file_type == file_type)
    return query.order_by(OrderFile.uploaded_at.desc()).all()

def delete_order_file(db: Session, file_id: int) -> bool:
    db_file = db.query(OrderFile).filter(OrderFile.id == file_id).first()
    if db_file:
        db.delete(db_file)
        db.commit()
        return True
    return False
```

- [ ] **步骤 3：创建文件上传路由**

```python
# backend/routers/order_file.py
from fastapi import APIRouter, Depends, UploadFile, File, HTTPException
from sqlalchemy.orm import Session
from typing import List, Optional
from app.database import get_db
from crud import order_file
from schemas.order_file import OrderFileCreate, OrderFileResponse
import os
import uuid
import shutil

router = APIRouter(prefix="/api/order-files", tags=["订单文件"])

@router.post("/upload/{pi_id}", response_model=OrderFileResponse)
async def upload_file(
    pi_id: int,
    file_type: str,
    file: UploadFile = File(...),
    db: Session = Depends(get_db)
):
    upload_dir = os.path.join("backend", "uploads", "order_files", str(pi_id))
    os.makedirs(upload_dir, exist_ok=True)

    ext = os.path.splitext(file.filename)[1]
    unique_name = f"{uuid.uuid4().hex}{ext}"
    file_path = os.path.join(upload_dir, unique_name)

    with open(file_path, "wb") as buffer:
        shutil.copyfileobj(file.file, buffer)

    file_data = OrderFileCreate(
        pi_id=pi_id,
        file_type=file_type,
        file_name=file.filename,
        file_path=file_path,
        file_size=file.size,
        file_ext=ext
    )
    return order_file.create_order_file(db, file_data)

@router.get("/{pi_id}", response_model=List[OrderFileResponse])
def get_files(pi_id: int, file_type: Optional[str] = None, db: Session = Depends(get_db)):
    return order_file.get_order_files(db, pi_id, file_type)

@router.delete("/{file_id}")
def delete_file(file_id: int, db: Session = Depends(get_db)):
    order_file.delete_order_file(db, file_id)
    return {"message": "删除成功"}
```

- [ ] **步骤 4：提交代码**

```bash
git add backend/models/order_file.py backend/schemas/order_file.py backend/crud/order_file.py backend/routers/order_file.py
git commit -m "feat: 添加订单文件上传功能"
```

---

## 任务 3：前端备忘录组件

**文件：**
- 创建：`client/widgets/memo_dialog.py`

- [ ] **步骤 1：创建备忘录弹窗组件**

```python
# client/widgets/memo_dialog.py
from PySide6.QtWidgets import QDialog, QVBoxLayout, QHBoxLayout, QPushButton, QTextEdit, QLabel, QListWidget, QListWidgetItem, QMessageBox
from PySide6.QtCore import Qt
from PySide6.QtGui import QFont

class MemoDialog(QDialog):
    def __init__(self, api_client, entity_type, entity_id, field_name, parent=None):
        super().__init__(parent)
        self.api_client = api_client
        self.entity_type = entity_type
        self.entity_id = entity_id
        self.field_name = field_name
        self.memos = []
        self.init_ui()
        self.load_memos()

    def init_ui(self):
        self.setWindowTitle(f"备忘录 - {self.field_name}")
        self.setMinimumSize(600, 500)

        layout = QVBoxLayout()

        title = QLabel(f"备忘录 - {self.field_name}")
        title.setFont(QFont("", 12, QFont.Weight.Bold))
        layout.addWidget(title)

        self.memo_list = QListWidget()
        self.memo_list.itemDoubleClicked.connect(self.edit_memo)
        layout.addWidget(self.memo_list)

        input_layout = QHBoxLayout()
        self.input_field = QTextEdit()
        self.input_field.setPlaceholderText("添加新记录...")
        self.input_field.setMaximumHeight(80)
        input_layout.addWidget(self.input_field)

        add_btn = QPushButton("添加")
        add_btn.clicked.connect(self.add_memo)
        input_layout.addWidget(add_btn)
        layout.addLayout(input_layout)

        close_btn = QPushButton("关闭")
        close_btn.clicked.connect(self.accept)
        layout.addWidget(close_btn)

        self.setLayout(layout)

    def load_memos(self):
        try:
            self.memos = self.api_client.get_memos(self.entity_type, self.entity_id, self.field_name)
            self.refresh_list()
        except Exception as e:
            QMessageBox.warning(self, "错误", f"加载失败: {e}")

    def refresh_list(self):
        self.memo_list.clear()
        for memo in self.memos:
            item_text = f"[{memo.get('created_at', '')}] {memo.get('content', '')}"
            item = QListWidgetItem(item_text)
            item.setData(Qt.UserRole, memo.get('id'))
            self.memo_list.addItem(item)

    def add_memo(self):
        content = self.input_field.toPlainText().strip()
        if not content:
            return
        try:
            self.api_client.create_memo({
                "entity_type": self.entity_type,
                "entity_id": self.entity_id,
                "field_name": self.field_name,
                "content": content
            })
            self.input_field.clear()
            self.load_memos()
        except Exception as e:
            QMessageBox.warning(self, "错误", f"添加失败: {e}")

    def edit_memo(self, item):
        memo_id = item.data(Qt.UserRole)
        new_content, ok = QInputDialog.getText(self, "编辑", "修改内容:", QLineEdit.Normal, item.text().split("] ", 1)[1] if "] " in item.text() else "")
        if ok and new_content:
            try:
                self.api_client.update_memo(memo_id, {"content": new_content})
                self.load_memos()
            except Exception as e:
                QMessageBox.warning(self, "错误", f"修改失败: {e}")
```

- [ ] **步骤 2：提交代码**

```bash
git add client/widgets/memo_dialog.py
git commit -m "feat: 添加前端备忘录组件"
```

---

## 任务 4：前端往来款面板

**文件：**
- 创建：`client/widgets/payment_dialog.py`

- [ ] **步骤 1：创建往来款弹窗组件**

```python
# client/widgets/payment_dialog.py
from PySide6.QtWidgets import QDialog, QVBoxLayout, QHBoxLayout, QPushButton, QTableWidget, QTableWidgetItem, QHeaderView
from PySide6.QtCore import Qt
from PySide6.QtGui import QFont

class PaymentDialog(QDialog):
    def __init__(self, api_client, pi_id, parent=None):
        super().__init__(parent)
        self.api_client = api_client
        self.pi_id = pi_id
        self.init_ui()
        self.load_data()

    def init_ui(self):
        self.setWindowTitle("往来款管理")
        self.setMinimumSize(700, 500)

        layout = QVBoxLayout()

        title = QLabel("往来款管理")
        title.setFont(QFont("", 12, QFont.Weight.Bold))
        layout.addWidget(title)

        customer_label = QLabel("客户付款记录")
        customer_label.setFont(QFont("", 10, QFont.Weight.Bold))
        layout.addWidget(customer_label)

        self.customer_table = QTableWidget()
        self.customer_table.setColumnCount(4)
        self.customer_table.setHorizontalHeaderLabels(["日期", "金额", "类型", "状态"])
        self.customer_table.horizontalHeader().setSectionResizeMode(QHeaderView.ResizeMode.Stretch)
        layout.addWidget(self.customer_table)

        supplier_label = QLabel("供应商付款记录")
        supplier_label.setFont(QFont("", 10, QFont.Weight.Bold))
        layout.addWidget(supplier_label)

        self.supplier_table = QTableWidget()
        self.supplier_table.setColumnCount(4)
        self.supplier_table.setHorizontalHeaderLabels(["日期", "金额", "类型", "状态"])
        self.supplier_table.horizontalHeader().setSectionResizeMode(QHeaderView.ResizeMode.Stretch)
        layout.addWidget(self.supplier_table)

        close_btn = QPushButton("关闭")
        close_btn.clicked.connect(self.accept)
        layout.addWidget(close_btn)

        self.setLayout(layout)

    def load_data(self):
        try:
            customer_payments = self.api_client.get_customer_payments_by_pi(self.pi_id)
            self.customer_table.setRowCount(len(customer_payments))
            for i, payment in enumerate(customer_payments):
                self.customer_table.setItem(i, 0, QTableWidgetItem(str(payment.get('payment_date', ''))))
                self.customer_table.setItem(i, 1, QTableWidgetItem(str(payment.get('amount', 0))))
                self.customer_table.setItem(i, 2, QTableWidgetItem(payment.get('payment_type', '')))
                self.customer_table.setItem(i, 3, QTableWidgetItem(payment.get('status', '')))

            supplier_payments = self.api_client.get_supplier_payments_by_pi(self.pi_id)
            self.supplier_table.setRowCount(len(supplier_payments))
            for i, payment in enumerate(supplier_payments):
                self.supplier_table.setItem(i, 0, QTableWidgetItem(str(payment.get('payment_date', ''))))
                self.supplier_table.setItem(i, 1, QTableWidgetItem(str(payment.get('amount', 0))))
                self.supplier_table.setItem(i, 2, QTableWidgetItem(payment.get('payment_type', '')))
                self.supplier_table.setItem(i, 3, QTableWidgetItem(payment.get('status', '')))
        except Exception as e:
            print(f"加载失败: {e}")
```

- [ ] **步骤 2：提交代码**

```bash
git add client/widgets/payment_dialog.py
git commit -m "feat: 添加前端往来款面板组件"
```

---

## 任务 5：订单总表视图切换优化

**文件：**
- 修改：`client/main.py`

- [ ] **步骤 1：更新订单总表视图切换逻辑**

参考之前的代码，确保：
- 默认全屏显示订单列表
- 点击订单后显示详情（顶部标题栏 + 返回按钮）
- 返回按钮正确切换视图

- [ ] **步骤 2：添加 API 方法**

```python
# client/api/client.py
def get_memos(self, entity_type: str, entity_id: int, field_name: str = None) -> List[Dict]:
    params = {"entity_type": entity_type, "entity_id": entity_id}
    if field_name:
        params["field_name"] = field_name
    return self.get("/memos", params=params)

def create_memo(self, data: Dict) -> Dict:
    return self.post("/memos", data)

def update_memo(self, memo_id: int, data: Dict) -> Dict:
    return self.put(f"/memos/{memo_id}", data)

def get_order_files(self, pi_id: int, file_type: str = None) -> List[Dict]:
    path = f"/order-files/{pi_id}"
    if file_type:
        path += f"?file_type={file_type}"
    return self.get(path)

def upload_order_file(self, pi_id: int, file_type: str, file_path: str) -> Dict:
    with open(file_path, 'rb') as f:
        return self.upload(f"/order-files/upload/{pi_id}?file_type={file_type}", f)
```

- [ ] **步骤 3：提交代码**

```bash
git add client/main.py client/api/client.py
git commit -m "feat: 优化订单总表视图切换"
```

---

## 任务 6：Excel 导出功能

**文件：**
- 修改：`client/main.py`

- [ ] **步骤 1：实现 Excel 导出**

```python
# client/main.py
def export_order_summary_excel(self):
    """导出订单总表到 Excel"""
    from PySide6.QtWidgets import QFileDialog
    import pandas as pd

    file_path, _ = QFileDialog.getSaveFileName(
        self,
        "导出订单总表",
        "",
        "Excel文件 (*.xlsx)"
    )
    if not file_path:
        return

    try:
        orders = self._order_summary_filtered or []
        data = []
        for order in orders:
            items = order.get('items', [])
            for idx, item in enumerate(items):
                row = {
                    "订单日期": order.get('order_date', ''),
                    "ORDER NO.": order.get('order_no', ''),
                    "客户产品编号": item.get('customer_code', ''),
                    "OE号": item.get('oe_number', ''),
                    "数量": item.get('quantity', 0),
                    "报价": item.get('unit_price', 0),
                    "合计金额": item.get('total_price', 0),
                    "品牌": item.get('brand', ''),
                }
                data.append(row)

        df = pd.DataFrame(data)
        df.to_excel(file_path, index=False)
        QMessageBox.information(self, "成功", f"已导出 {len(data)} 条记录")
    except Exception as e:
        QMessageBox.warning(self, "错误", f"导出失败: {e}")
```

- [ ] **步骤 2：提交代码**

```bash
git add client/main.py
git commit -m "feat: 添加订单总表Excel导出功能"
```

---

## 实施优先级

| 任务 | 优先级 | 说明 |
|:---:|:---:|:---|
| 1 | P1 | 备忘录数据模型和 API |
| 2 | P1 | 文件上传数据模型和 API |
| 3 | P2 | 前端备忘录组件 |
| 4 | P2 | 前端往来款面板 |
| 5 | P3 | 订单总表视图切换优化 |
| 6 | P4 | Excel 导出功能 |

---

**计划已完成并保存到 `docs/superpowers/plans/2026-05-08-订单管理总表.md`。**

**两种执行方式：**

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

**选哪种方式？**
