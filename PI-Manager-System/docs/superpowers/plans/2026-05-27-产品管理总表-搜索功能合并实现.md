# 产品管理总表 - 搜索功能合并实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 subagent-driven-development（推荐）或 executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 合并产品管理总表中两个重复的搜索输入框，统一搜索入口，简化界面

**架构：** 在工具栏搜索区域恢复 `search_input`，精简筛选栏只保留客户筛选，添加独立的"筛选"按钮

**技术栈：** PySide6, Python

---

## 文件修改清单

| 文件 | 职责 |
|------|------|
| `client/main.py` | 产品管理总表的所有搜索相关UI和方法 |

---

## 任务清单

### 任务 1：恢复 search_input 搜索框

**文件：** `client/main.py:3654-3663`

- [ ] **步骤 1：取消注释 search_input**

将以下注释代码取消注释：

```python
# 搜索框
search_layout = QHBoxLayout()
search_layout.setSpacing(10)

self.search_input = QLineEdit()                           # 恢复
self.search_input.setPlaceholderText("搜索（OE号/产品编号/工厂编号/品牌/描述）")
self.search_input.setFixedWidth(250)
search_layout.addWidget(self.search_input)                # 恢复

self.category_filter = QComboBox()
self.category_filter.addItem("全部分类", 0)
search_layout.addWidget(self.category_filter)
```

- [ ] **步骤 2：验证代码结构**

运行客户端检查界面是否正常显示搜索框

- [ ] **步骤 3：Commit**

```bash
git add client/main.py
git commit -m "feat(产品管理): 恢复搜索框 search_input"
```

---

### 任务 2：精简筛选栏

**文件：** `client/main.py:3707-3742`

- [ ] **步骤 1：删除重复的 category_filter 下拉框**

删除以下代码（已在工具栏有 `category_filter`）：

```python
filter_layout.addWidget(QLabel("类别:"))
self.product_category_filter = QComboBox()
self.product_category_filter.setFixedWidth(150)
self.product_category_filter.addItem("全部类别", "")
filter_layout.addWidget(self.product_category_filter)
```

- [ ] **步骤 2：删除重复的 product_search_input**

删除以下代码（已在工具栏有 `search_input`）：

```python
filter_layout.addWidget(QLabel("搜索:"))
self.product_search_input = QLineEdit()
self.product_search_input.setPlaceholderText("产品名称/OE号/编号...")
self.product_search_input.setFixedWidth(200)
filter_layout.addWidget(self.product_search_input)
```

- [ ] **步骤 3：删除 product_status_label**

删除以下代码：

```python
self.product_status_label = QLabel("")
self.product_status_label.setStyleSheet("color: #6b7280;")
filter_layout.addWidget(self.product_status_label)
```

- [ ] **步骤 4：添加筛选按钮**

在 `refresh_btn` 前添加筛选按钮：

```python
filter_btn = QPushButton("筛选")
filter_btn.clicked.connect(self.filter_by_customer)
filter_btn.setStyleSheet("""
    QPushButton {
        background-color: #e5e7eb;
        color: #374151;
        border: none;
        border-radius: 6px;
        padding: 8px 16px;
    }
    QPushButton:hover { background-color: #d1d5db; }
""")
filter_layout.addWidget(filter_btn)
```

- [ ] **步骤 5：Commit**

```bash
git add client/main.py
git commit -m "refactor(产品管理): 精简筛选栏，删除重复组件"
```

---

### 任务 3：实现 filter_by_customer 方法

**文件：** `client/main.py`（新增方法）

- [ ] **步骤 1：找到合适位置添加新方法**

在 `filter_products` 方法附近添加：

```python
def filter_by_customer(self):
    """根据客户筛选产品"""
    customer_id = self.product_customer_filter.currentData()
    if customer_id == 0:
        self.search_products()
        return

    keyword = self.search_input.text().strip()
    category_id = self.category_filter.currentData()
    category_id = category_id if category_id != 0 else None

    try:
        products = self.api_client.search_products(keyword, category_id, customer_id)
        self._populate_products_table(products)
    except Exception as e:
        self._logger.error(f"客户筛选失败: {str(e)}")
```

- [ ] **步骤 2：测试筛选功能**

运行客户端，选择客户后点击筛选按钮，验证过滤结果

- [ ] **步骤 3：Commit**

```bash
git add client/main.py
git commit -m "feat(产品管理): 添加 filter_by_customer 方法"
```

---

### 任务 4：清理相关旧代码

**文件：** `client/main.py`

- [ ] **步骤 1：删除 product_search_input 的事件绑定**

找到并删除以下代码：

```python
self.product_search_input.textChanged.disconnect()
```

- [ ] **步骤 2：删除 product_search_input 的事件连接**

找到并删除以下代码：

```python
self.product_search_input.textChanged.connect(self.filter_products)
```

- [ ] **步骤 3：删除 filter_products 中的 product_search_input 引用**

修改 `filter_products` 方法，删除搜索关键词部分（因为关键词搜索已移到工具栏）：

```python
def filter_products(self):
    """根据筛选条件过滤产品列表"""
    if not self._product_cache:
        return

    customer_id = self.product_customer_filter.currentData()
    search_keyword = self.product_search_input.text().strip().lower()  # 删除这行
    # ... 后续代码中的搜索关键词部分也删除
```

- [ ] **步骤 4：Commit**

```bash
git add client/main.py
git commit -m "chore(产品管理): 清理相关旧代码"
```

---

### 任务 5：验证完整功能

**文件：** `client/main.py`

- [ ] **步骤 1：验证搜索功能**

1. 输入关键词 → 点击搜索 → 验证结果
2. 选择分类 → 点击搜索 → 验证结果
3. 选择分类 + 关键词 → 点击搜索 → 验证结果

- [ ] **步骤 2：验证客户筛选功能**

1. 先执行搜索
2. 选择客户 → 点击筛选 → 验证过滤结果
3. 清空客户选择 → 点击筛选 → 验证恢复全部结果

- [ ] **步骤 3：验证重置功能**

点击重置按钮 → 验证搜索框和分类都恢复默认

- [ ] **步骤 4：Commit 最终版本**

```bash
git add client/main.py
git commit -m "feat(产品管理): 完成搜索功能合并"
```

---

## 验收标准

- [ ] 搜索框只有一个，位于工具栏区域
- [ ] 分类筛选通过工具栏下拉框实现
- [ ] 客户筛选保留在筛选栏
- [ ] 点击搜索按钮能正确调用 `search_products`
- [ ] 选择客户后点击筛选能正确过滤产品列表
- [ ] 重置按钮能清空搜索条件
- [ ] 界面布局整洁，无重复元素

---

## 执行方式

**计划已完成。两种执行方式：**

**1. 子代理驱动（推荐）** - 每个任务调度一个新的子代理，任务间进行审查，快速迭代

**2. 内联执行** - 在当前会话中使用 executing-plans 执行任务，批量执行并设有检查点

**选哪种方式？**