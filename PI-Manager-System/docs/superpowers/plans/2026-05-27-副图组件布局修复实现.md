# 副图组件布局修复实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 subagent-driven-development（推荐）或 executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 修复 SubImageWidget 副图组件，将布局从横向改为纵向网格布局，确保添加/删除按钮正常显示

**架构：** 使用 QGridLayout 实现网格布局，每行显示4张图片，每张图片下方有独立的删除按钮

**技术栈：** PySide6, Python

---

## 文件修改清单

| 文件 | 职责 |
|------|------|
| `client/widgets/customer_product_dialog.py` | SubImageWidget 组件的 UI 布局修改 |

---

## 任务清单

### 任务 1：修改滚动区域配置

**文件：** `client/widgets/customer_product_dialog.py:113-138`（SubImageWidget.__init__）

- [ ] **步骤 1：导入 QGridLayout**

在文件顶部 `from PySide6.QtWidgets import` 列表中添加 `QGridLayout`

```python
from PySide6.QtWidgets import (
    QDialog, QVBoxLayout, QHBoxLayout, QFormLayout, QLabel, QLineEdit,
    QTextEdit, QComboBox, QPushButton, QWidget, QGroupBox, QTableWidget,
    QTableWidgetItem, QHeaderView, QAbstractItemView, QCheckBox, QScrollArea,
    QMessageBox, QInputDialog, QApplication, QGridLayout
)
```

- [ ] **步骤 2：修改滚动区域配置**

将 `__init__` 中的滚动区域设置从：
```python
scroll = QScrollArea()
scroll.setFixedHeight(70)
scroll.setWidgetResizable(False)
scroll.setHorizontalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)
scroll.setVerticalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAlwaysOff)
```

改为：
```python
scroll = QScrollArea()
scroll.setMinimumHeight(150)
scroll.setWidgetResizable(True)
scroll.setHorizontalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAlwaysOff)
scroll.setVerticalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)
```

- [ ] **步骤 3：Commit**

```bash
git add client/widgets/customer_product_dialog.py
git commit -m "fix(副图组件): 调整滚动区域为垂直滚动模式"
```

---

### 任务 2：重构 _rebuild 方法

**文件：** `client/widgets/customer_product_dialog.py:157-207`（SubImageWidget._rebuild）

- [ ] **步骤 1：替换 _rebuild 方法体**

将整个 `_rebuild` 方法替换为网格布局版本：

```python
def _rebuild(self):
    """重建图片显示 - 使用网格布局"""
    # 清空现有内容
    while self.container_layout.count():
        item = self.container_layout.takeAt(0)
        if item.widget():
            item.widget().deleteLater()
    
    # 清理线程
    for worker in self.image_workers:
        if not worker.isFinished():
            worker.quit()
    self.image_workers.clear()
    self.image_labels.clear()
    self.loading_labels.clear()
    
    # 创建网格容器
    grid_widget = QWidget()
    grid_layout = QGridLayout(grid_widget)
    grid_layout.setSpacing(10)
    grid_layout.setContentsMargins(0, 0, 0, 0)
    
    # 每行显示数量
    cols = 4
    
    for idx, url in enumerate(self.images):
        row = idx // cols
        col = idx % cols
        
        img_widget = QWidget()
        img_layout = QVBoxLayout(img_widget)
        img_layout.setContentsMargins(0, 0, 0, 0)
        img_layout.setSpacing(4)
        
        # 图片
        img_label = QLabel()
        img_label.setFixedSize(100, 100)
        img_label.setStyleSheet("border: 1px solid #e5e7eb; border-radius: 4px; background-color: #f3f4f6;")
        img_label.setAlignment(Qt.AlignCenter)
        img_label.setText("加载中...")
        img_label.setProperty("url", url)
        self.image_labels[url] = img_label
        self.loading_labels[url] = True
        img_layout.addWidget(img_label)
        
        # 删除按钮
        delete_btn = QPushButton("删除")
        delete_btn.setFixedHeight(28)
        delete_btn.setStyleSheet("""
            QPushButton {
                background-color: #ef4444;
                color: white;
                border: none;
                border-radius: 4px;
                font-size: 12px;
            }
            QPushButton:hover {
                background-color: #dc2626;
            }
        """)
        delete_btn.clicked.connect(lambda checked, i=idx: self.remove_image(i))
        img_layout.addWidget(delete_btn)
        
        grid_layout.addWidget(img_widget, row, col)
        self._load_image_async(url)
    
    # 计算添加按钮位置
    if self.images:
        add_row = len(self.images) // cols
    else:
        add_row = 0
    
    # 添加按钮
    add_widget = QWidget()
    add_layout = QVBoxLayout(add_widget)
    add_layout.setContentsMargins(0, 0, 0, 0)
    add_layout.setSpacing(4)
    
    add_label = QLabel()
    add_label.setFixedSize(100, 100)
    add_label.setAlignment(Qt.AlignCenter)
    add_label.setText("+")
    add_label.setStyleSheet("""
        border: 2px dashed #d1d5db;
        border-radius: 4px;
        background-color: #f9fafb;
        color: #9ca3af;
        font-size: 32px;
    """)
    add_layout.addWidget(add_label)
    
    add_btn = QPushButton("添加图片")
    add_btn.setFixedHeight(28)
    add_btn.setStyleSheet("""
        QPushButton {
            background-color: #f3f4f6;
            color: #374151;
            border: none;
            border-radius: 4px;
            font-size: 12px;
        }
        QPushButton:hover {
            background-color: #e5e7eb;
        }
    """)
    add_btn.clicked.connect(self._add_new_image)
    add_layout.addWidget(add_btn)
    
    grid_layout.addWidget(add_widget, add_row, 0, 1, 1)  # 添加到下一行开头
    
    # 替换容器内容
    while self.container_layout.count():
        self.container_layout.takeAt(0)
    
    self.container_layout.addWidget(grid_widget)
```

- [ ] **步骤 2：Commit**

```bash
git add client/widgets/customer_product_dialog.py
git commit -m "refactor(副图组件): 使用网格布局替代横向布局"
```

---

### 任务 3：调整图片加载回调

**文件：** `client/widgets/customer_product_dialog.py:210-225`（SubImageWidget._on_image_loaded）

- [ ] **步骤 1：修改 _on_image_loaded 方法**

将 `_on_image_loaded` 方法修改为适配新尺寸：

```python
def _on_image_loaded(self, url, pixmap):
    if url in self.image_labels and url in self.loading_labels:
        label = self.image_labels[url]
        # 缩放到100x100，保持比例
        scaled_pixmap = pixmap.scaled(100, 100, Qt.KeepAspectRatio, Qt.SmoothTransformation)
        label.setPixmap(scaled_pixmap)
        label.setText("")
        del self.loading_labels[url]
```

- [ ] **步骤 2：Commit**

```bash
git add client/widgets/customer_product_dialog.py
git commit -m "fix(副图组件): 适配新图片尺寸100x100"
```

---

### 任务 4：测试验证

**文件：** `client/widgets/customer_product_dialog.py`

- [ ] **步骤 1：启动客户端测试**

1. 启动后端服务器
2. 启动客户端
3. 打开产品管理总表
4. 编辑或新增客户产品
5. 进入图片区域，验证副图组件显示正常

- [ ] **步骤 2：验证功能**

1. 点击"添加图片"按钮，验证能打开文件选择对话框
2. 添加图片后，验证图片和"删除"按钮都正常显示
3. 点击"删除"按钮，验证图片被正确移除
4. 滚动查看多行图片，验证滚动正常

- [ ] **步骤 3：Commit**

```bash
git add client/widgets/customer_product_dialog.py
git commit -m "test(副图组件): 验证网格布局功能正常"
```

---

## 验收标准

- [ ] 每张图片下方有清晰的"删除"按钮
- [ ] 有明确的"添加图片"按钮
- [ ] 图片和按钮不会被截断
- [ ] 支持垂直滚动查看多行图片
- [ ] 按钮有 hover 效果
- [ ] 图片加载中显示"加载中..."文字
- [ ] `main_image_label` 支持点击触发上传
- [ ] "上传主图"按钮正常工作
- [ ] 上传成功后正确显示图片
- [ ] "清除"按钮正常工作

---

## 任务 5：修复主图点击上传功能

**文件：** `client/widgets/customer_product_dialog.py`

- [ ] **步骤 1：创建 ClickableLabel 类**

在 `BatchImportDialog` 类之前添加：

```python
class ClickableLabel(QLabel):
    """可点击的 QLabel"""
    clicked = Signal()

    def __init__(self, parent=None):
        super().__init__(parent)

    def mousePressEvent(self, event):
        if event.button() == Qt.LeftButton:
            self.clicked.emit()
        super().mousePressEvent(event)
```

- [ ] **步骤 2：导入 Signal**

确保文件顶部导入：
```python
from PySide6.QtCore import Qt, QTimer, Signal, QThread
```

- [ ] **步骤 3：替换 main_image_label 为 ClickableLabel**

在 `init_ui` 方法中找到：
```python
self.main_image_label = QLabel()
```

替换为：
```python
self.main_image_label = ClickableLabel()
```

- [ ] **步骤 4：绑定点击事件**

在 `self.main_image_label` 初始化后添加：
```python
self.main_image_label.setFixedSize(200, 200)
self.main_image_label.setStyleSheet("border: 2px dashed #d1d5db; background-color: #f9fafb;")
self.main_image_label.setAlignment(Qt.AlignCenter)
self.main_image_label.setText("点击上传主图")
self.main_image_label.setCursor(Qt.PointingHandCursor)
self.main_image_label.clicked.connect(self.upload_main_image)  # 添加这行
```

- [ ] **步骤 5：测试主图上传**

1. 启动客户端
2. 打开客户产品编辑对话框
3. 点击主图区域，验证打开文件选择对话框
4. 选择图片后验证上传和显示

- [ ] **步骤 6：Commit**

```bash
git add client/widgets/customer_product_dialog.py
git commit -m "fix(客户产品): 修复主图点击上传功能"
```

---

## 注意事项

1. **QGridLayout 列跨度问题**：当图片数量不是4的倍数时，添加按钮要放在下一行开头
2. **lambda 闭包问题**：删除按钮的 `lambda: self.remove_image(i)` 需要使用默认参数 `i=idx` 避免闭包问题
3. **异步线程清理**：确保 `_rebuild` 时正确清理未完成的 QThread
4. **ClickableLabel**：确保 Signal 已从 PySide6.QtCore 导入

---

## 执行方式

**计划已完成。两种执行方式：**

**1. 子代理驱动（推荐）** - 每个任务调度一个新子代理，任务间进行审查

**2. 内联执行** - 在当前会话批量执行，设有检查点

**选哪种方式？**